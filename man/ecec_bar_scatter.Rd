% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplots.R
\name{ecec_bar_scatter}
\alias{ecec_bar_scatter}
\title{Barplot with overlaid scatter points}
\usage{
ecec_bar_scatter(
  data,
  x,
  y,
  fill = NULL,
  bar_stat = "summary",
  bar_fun = "mean",
  bar_colour = "#4472C4",
  orientation = "vertical",
  width = 0.7,
  jitter_width = 0.15,
  point_size = 2,
  point_colour = "#222222",
  point_alpha = 0.6,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x_label = NULL,
  y_label = NULL,
  x_angle = 0,
  y_angle = 0,
  text_scale = 1,
  legend_nrow = NULL,
  legend_ncol = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Column name for the categorical axis.}

\item{y}{Column name for the numeric value used for both the
bars and the scatter points.}

\item{fill}{Optional column name used to fill the bars (default
\code{NULL}: a single fill colour).}

\item{bar_stat}{Statistic for the bars: \code{"identity"} uses the raw
\code{y} values (one row per bar), \code{"summary"} computes the mean.
Default \code{"summary"}.}

\item{bar_fun}{Summary function passed to
\code{geom_bar(fun = )} when \code{bar_stat = "summary"}.  Default
\code{"mean"}.}

\item{bar_colour}{Fill colour for bars when \code{fill} is \code{NULL}
(default \code{"#4472C4"}).}

\item{orientation}{\code{"vertical"} (default) or \code{"horizontal"}.}

\item{width}{Bar width (0–1, default \code{0.6}).}

\item{jitter_width}{Horizontal spread of jittered points (default
\code{0.15}).}

\item{point_size}{Size of scatter points (default \code{2}).}

\item{point_colour}{Colour of scatter points (default \code{"#222222"}).}

\item{point_alpha}{Transparency of scatter points (default \code{0.6}).}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{caption}{Plot caption.}

\item{x_label}{Axis label for the category axis; defaults to the
column name.}

\item{y_label}{Axis label for the value axis; defaults to the column
name.}

\item{x_angle}{Rotation angle for x-axis labels in degrees (default \code{0}).}

\item{y_angle}{Rotation angle for y-axis labels in degrees (default \code{0}).}

\item{text_scale}{Scaling factor for text sizes (default \code{1}).}

\item{legend_nrow}{Number of rows in the legend (default \code{NULL}: automatic).}

\item{legend_ncol}{Number of columns in the legend (default \code{NULL}: automatic).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a barplot (showing e.g. a group mean or total) with individual
data points overlaid as a jittered scatter.  Useful for showing both
summary statistics and the underlying distribution.  Supports both vertical
(default) and horizontal orientations.
}
\examples{
ecec_bar_scatter(mtcars, x = "cyl", y = "mpg",
                 title = "MPG by Number of Cylinders")

# Horizontal with fill
ecec_bar_scatter(mtcars, x = "cyl", y = "mpg", fill = "cyl",
                 orientation = "horizontal",
                 title = "Horizontal: MPG by Cylinders")
}
