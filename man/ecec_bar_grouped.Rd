% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/barplots.R
\name{ecec_bar_grouped}
\alias{ecec_bar_grouped}
\title{Grouped (non-stacked) barplot}
\usage{
ecec_bar_grouped(
  data,
  x,
  y,
  fill,
  orientation = "vertical",
  width = 0.7,
  title = NULL,
  subtitle = NULL,
  caption = NULL,
  x_label = NULL,
  y_label = NULL,
  x_angle = 0,
  y_angle = 0,
  text_scale = 1,
  legend_nrow = NULL,
  legend_ncol = NULL
)
}
\arguments{
\item{data}{A data frame.}

\item{x}{Column name for the categorical axis.}

\item{y}{Column name for the numeric value.}

\item{fill}{Column name used to define and colour the bar groups.}

\item{orientation}{\code{"vertical"} (default) or \code{"horizontal"}.}

\item{width}{Bar width (0–1, default \code{0.7}).}

\item{title}{Plot title.}

\item{subtitle}{Plot subtitle.}

\item{caption}{Plot caption.}

\item{x_label}{Axis label for the category axis; defaults to the column
name.}

\item{y_label}{Axis label for the value axis; defaults to the column
name.}

\item{x_angle}{Rotation angle for x-axis labels in degrees (default \code{0}).}

\item{y_angle}{Rotation angle for y-axis labels in degrees (default \code{0}).}

\item{text_scale}{Scaling factor for text sizes (default \code{1}).}

\item{legend_nrow}{Number of rows in the legend (default \code{NULL}: automatic).}

\item{legend_ncol}{Number of columns in the legend (default \code{NULL}: automatic).}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a side-by-side barplot where bars within each category are placed
next to each other rather than stacked.  Supports both vertical (default)
and horizontal orientations.
}
\examples{
df <- data.frame(
  category = rep(c("A", "B", "C"), each = 2),
  group    = rep(c("G1", "G2"), times = 3),
  value    = c(10, 15, 8, 12, 9, 14)
)
ecec_bar_grouped(df, x = "category", y = "value", fill = "group")

# Horizontal
ecec_bar_grouped(df, x = "category", y = "value", fill = "group",
                 orientation = "horizontal")
}
